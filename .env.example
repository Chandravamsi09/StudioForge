# Application
NODE_ENV=development
PORT=4000
API_PREFIX=api/v1
CORS_ORIGIN=http://localhost:3000

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=studioforge
DB_PASSWORD=studioforge_secret_dev
DB_DATABASE=studioforge_db
DB_SYNCHRONIZE=false
DB_LOGGING=true

# Redis Cache & Sessions
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT Authentication
JWT_SECRET=studioforge_enterprise_jwt_super_secure_secret_key_2026
JWT_EXPIRATION=1d
JWT_REFRESH_SECRET=studioforge_refresh_token_super_secure_secret_key_2026
JWT_REFRESH_EXPIRATION=7d

# Billing & Default Limits
FREE_TIER_MAX_SEATS=5
PRO_TIER_MAX_SEATS=25
ENTERPRISE_TIER_MAX_SEATS=1000
